using UnityEngine;
using System.Collections;

namespace character
{
    public class KnightAnimation : KnightBase
    {

        // Use this for initialization
        void Start()
        {
            //_charactor = GameObject.Find("character_knight");
            //_animator = GetComponent<Animator>();
        }

        // Update is called once per frame
        void Update()
        {
            //pos = _charactor.rigidbody2D.position;   // 座標を取得
            //vel = _charactor.rigidbody2D.velocity;   // 速度を取得

            //// アニメーション
            //BaseAnimation();
        }
    }
}
